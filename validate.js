function validate() {
    var inputs = document.getElementById("form").getElementsByTagName("input");
  //  for(var x = 0; x<inputs.length; x++) {
      //if (inputs[x].value > 0) {
      alert("PLACEHOLDER");
      return false;
    //  }
}
